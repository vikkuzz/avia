import React from 'react';

import './Tabs.scss';

const Tabs = () => (
  <div className="tabs">
    <button type="submit" className="tabs__btn">
      дешево
    </button>
    <button type="submit" className="tabs__btn">
      быстро
    </button>
  </div>
);
export default Tabs;
